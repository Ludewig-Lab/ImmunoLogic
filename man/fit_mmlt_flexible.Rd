% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_mmlt_flexible.R
\name{fit_mmlt_flexible}
\alias{fit_mmlt_flexible}
\title{Flexible Multivariate Maximum Likelihood Transformation Model (MMLT)}
\usage{
fit_mmlt_flexible(
  dataframe,
  impute = F,
  seed = 490,
  marginal_formula = "~ 1",
  multivariate_formula = "~ 1",
  continuous_vars = c(),
  discrete_vars = c(),
  exclude_vars = c(),
  continuous_formula = "default",
  order = 6,
  automatic = T,
  auto_threshold = 5,
  verbose = T,
  ...
)
}
\arguments{
\item{dataframe}{Data frame containing the variables to be modeled.}

\item{impute}{Logical. Whether to impute missing data using \code{mice}. Default is \code{FALSE}.}

\item{seed}{Random seed for reproducibility.}

\item{marginal_formula}{Formula for the marginal models.}

\item{multivariate_formula}{Formula for the multivariate dependency.}

\item{continuous_vars}{Character vector of continuous variable names.}

\item{discrete_vars}{Character vector of discrete variable names.}

\item{exclude_vars}{Variables to exclude from modeling.}

\item{continuous_formula}{Type of continuous response handling: \code{"default"}, \code{"interval"}, or \code{"ordered"}.}

\item{order}{Polynomial order for the Box–Cox basis.}

\item{automatic}{Logical. If \code{TRUE}, classify variables automatically.}

\item{auto_threshold}{Integer. Threshold for automatic discrete classification (default = 5).}
}
\value{
A list containing:
\item{object}{The fitted MMLT model.}
\item{coef_corr}{Estimated correlation matrix.}
\item{coef_lambda}{Model coefficients.}
\item{standard_errors_lambda}{Standard errors of coefficients.}
\item{p_values}{P-values for all coefficients.}
\item{p_matrix}{Matrix of pairwise p-values.}
\item{AIC, BIC, logLik}{Model fit statistics.}
\item{automatic_classification}{Information about auto-classified variables (if used).}
}
\description{
Fits a flexible multivariate maximum likelihood transformation (MMLT) model
using the \code{tram} package. Handles automatic variable classification into
continuous or discrete types, missing data imputation, and model post-processing.
}
\details{
This function wraps a multi-step modeling pipeline:
data cleaning, optional imputation, automatic variable classification,
marginal model fitting for each variable, and final multivariate model fitting.
It outputs a structured list with results and diagnostics.
}
\examples{
\dontrun{
library(tram)
data <- data.frame(
  x1 = rnorm(100),
  x2 = rnorm(100),
  x3 = sample(1:5, 100, TRUE)
)
result <- fit_mmlt_flexible(data, automatic = TRUE)
}
}
\author{
Roman Stadler
}
